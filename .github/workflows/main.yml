name: 🚀 Build & Deploy (Next + Nest)

on:
  push:
    branches:
      - nestjs_backend
  workflow_dispatch:

jobs:
  build:
    name: 🏗 Build
    runs-on: ubuntu-latest
    env:
      CI: true

    steps:
      - name: 🚚 Checkout
        uses: actions/checkout@v4

      # Use Node 20 LTS for stability
      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      # ---------- FRONTEND BUILD ----------
      - name: 📦 Install & build frontend (standalone)
        working-directory: frontend
        run: |
          npm ci
          npm run build
          test -f .next/standalone/server.js

      - name: 📦 Pack frontend artifact
        run: |
          mkdir -p artifacts
          tar -czf artifacts/frontend.tgz \
            -C frontend \
            .next/standalone \
            .next/static \
            public \
            package.json \
            package-lock.json

      # ---------- BACKEND BUILD ----------
      - name: 📦 Install & build backend
        working-directory: backend
        run: |
          npm ci
          npx prisma generate
          npm run build
          test -f dist/main.js

      - name: 📦 Pack backend artifact
        run: |
          mkdir -p artifacts
          tar -czf artifacts/backend.tgz \
            -C backend \
            dist \
            prisma \
            package.json \
            package-lock.json \
            prisma.config.ts || true

      - name: ⬆️ Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: deploy-bundles
          path: artifacts/*.tgz

  deploy:
    name: 🚀 Deploy to VPS
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/nestjs_backend'

    steps:
      - name: ⬇️ Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: deploy-bundles
          path: artifacts

      # Upload tarballs (tar avoids ".next hidden dir" problems completely)
      - name: 📤 Upload bundles to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          source: "artifacts/frontend.tgz,artifacts/backend.tgz"
          target: "/tmp/jwell_deploy"

      - name: 🚀 Deploy & restart (PM2)
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            set -e

            APP_ROOT="/var/www/jwell_b2b"
            FRONT_DIR="$APP_ROOT/frontend"
            BACK_DIR="$APP_ROOT/backend"
            BKP_DIR="$APP_ROOT/backups"
            TS="$(date +%Y%m%d_%H%M%S)"

            sudo mkdir -p "$FRONT_DIR" "$BACK_DIR" "$BKP_DIR"

            # ---------------- BACKUP ----------------
            if [ -d "$FRONT_DIR/.next" ] || [ -f "$FRONT_DIR/package.json" ]; then
              sudo tar -czf "$BKP_DIR/frontend_$TS.tgz" -C "$APP_ROOT" frontend || true
              echo "✅ Frontend backup: $BKP_DIR/frontend_$TS.tgz"
            fi

            if [ -d "$BACK_DIR/dist" ] || [ -f "$BACK_DIR/package.json" ]; then
              sudo tar -czf "$BKP_DIR/backend_$TS.tgz" -C "$APP_ROOT" backend || true
              echo "✅ Backend backup: $BKP_DIR/backend_$TS.tgz"
            fi

            # ---------------- DEPLOY FRONTEND ----------------
            sudo rm -rf "$FRONT_DIR"/*
            sudo tar -xzf /tmp/jwell_deploy/artifacts/frontend.tgz -C "$FRONT_DIR"

            # ---------------- DEPLOY BACKEND ----------------
            sudo rm -rf "$BACK_DIR"/*
            sudo tar -xzf /tmp/jwell_deploy/artifacts/backend.tgz -C "$BACK_DIR"

            # ---------------- INSTALL PROD DEPS (backend only) ----------------
            # Frontend standalone includes its own minimal node_modules inside .next/standalone
            cd "$BACK_DIR"
            sudo npm ci --omit=dev
            sudo npx prisma generate
            sudo npx prisma migrate deploy 2>/dev/null || true

            # ---------------- PM2 START/RESTART ----------------
            # Frontend standalone entry
            FRONT_ENTRY="$FRONT_DIR/.next/standalone/server.js"
            # Ensure Next can find static assets
            # Next standalone expects .next/static and public relative to project root; we deployed them.

            if ! command -v pm2 >/dev/null 2>&1; then
              echo "❌ pm2 not found on server. Install pm2 first."
              exit 1
            fi

            # Restart or start frontend
            pm2 describe jwell_frontend >/dev/null 2>&1 \
              && pm2 restart jwell_frontend \
              || pm2 start node --name jwell_frontend -- "$FRONT_ENTRY"

            # Restart or start backend
            pm2 describe jwell_backend >/dev/null 2>&1 \
              && pm2 restart jwell_backend \
              || pm2 start "$BACK_DIR/dist/main.js" --name jwell_backend

            pm2 save

            echo "✅ Deploy complete: frontend + backend restarted"
